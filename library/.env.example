# User Management Library Environment Configuration
# Copy this file to .env and configure for your environment

# Provider Selection (set one)
# AWS_COGNITO=true        # Enable direct Cognito API access (IPv6 environments)
# AWS_LAMBDA=true         # Enable Lambda proxy access (private subnets)

# AWS Configuration
AWS_REGION=ap-northeast-1
# AWS_PROFILE=default     # Optional: AWS CLI profile to use

# Cognito Configuration (required when AWS_COGNITO=true)
USERPOOL_ID=ap-northeast-1_xxxxxxxxx
CLIENT_ID=your_client_id
SECRET=your_client_secret

# Lambda Configuration (required when AWS_LAMBDA=true)
LAMBDA_FUNCTION_NAME=usermgr

# Test Configuration
# Set these for running tests
# AWS_ACCESS_KEY_ID=your_access_key  # Optional if using AWS_PROFILE
# AWS_SECRET_ACCESS_KEY=your_secret_key  # Optional if using AWS_PROFILE