import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

/**
 * AWS設定フォームコンポーネント
 * このコンポーネントは実際の実装が完了するまでのプレースホルダーです
 */
export function SettingsForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>AWS/Cognito設定</CardTitle>
      </CardHeader>
      <CardContent>
        <p>AWS認証情報とCognitoユーザープール設定フォームがここに表示されます</p>
      </CardContent>
    </Card>
  );
}
